import React from 'react'
import {observer} from 'mobx-react'

export function TransactionStatus (props: any) {
  return (
    <div>
      Transaction
    </div>
  )
}

export default observer(TransactionStatus)
